#include "headers/libft.h"
#include "headers/minirt.h"

int		ft_strstrlen(char **str);

int main()
{
	char str[] = "9";
	char **split;

	if (ft_checkdoub(str))
		ft_printf("%s", "mal");
	else
		ft_printf("%s", "tot be");
	return (0);
}
